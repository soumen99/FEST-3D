!< Declare all the constants used by gradient diffusion model
module gradient_diffusion
  !< Declare all the constants used by gradient diffusion model
  use iso_fortran_env, only : wp => real64

  real(wp), parameter  :: sc    = 1
